import getStandings from 'klask-app/utils/standings';
import { module, test } from 'qunit';

module('Unit | Utility | standings');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getStandings();
  assert.ok(result);
});
