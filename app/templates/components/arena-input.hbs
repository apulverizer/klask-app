{{#paper-form onSubmit=(action joinNewArena name) as |form|}}
  {{paper-input class="paper-input-container" label="Enter Arena ID" value=name onChange=(action (mut name)) iconRight="send" enter=(action joinNewArena)}}
{{/paper-form}}
{{yield}}
