{{#paper-menu position="target-right target" as |menu|}}

  {{#menu.trigger}}
    {{#paper-button iconButton=true}}
      <img src="{{user.photourl}}"/>
    {{/paper-button}}
  {{/menu.trigger}}

  {{#menu.content class="menu-overflow-your-profile" width=4 as |content|}}

    {{#content.menu-item onClick="viewProfile"}}
      {{paper-icon "person" class="md-menu-align-target"}}
      <span>View profile</span>
    {{/content.menu-item}}

    {{#content.menu-item onClick="changeArena"}}
      {{paper-icon "swap_vert" class="md-menu-align-target"}}
      <span>Change arena</span>
    {{/content.menu-item}}

    {{#content.menu-item onClick="signOut"}}
      {{paper-icon "exit_to_app" class="md-menu-align-target"}}
      <span>Sign out</span>
    {{/content.menu-item}}


  {{/menu.content}}
{{/paper-menu}}

{{yield}}