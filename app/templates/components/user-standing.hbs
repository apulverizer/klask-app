{{!-- <span class="rank_num">{{rank}}</span>
<span class="mdl-list__item-primary-content">
    <img class="material-icons mdl-list__item-avatar" src="{{user.photourl}}"/>
    <span>{{user.name}}</span>
    <span class="mdl-list__item-sub-title">{{user.nickname}}</span>
</span>
<span class="mdl-list__item-secondary-content win_percentage">{{winPercentage}}%</span>
<span class="mdl-ripple"></span>
 --}}

{{#paper-item class="md-2-line" onClick="openSomething"}}
    <div class="left">
        <span>{{rank}}</span>
    </div>
    
    <img src={{user.photourl}} alt={{item.name}} class="md-avatar">

    <div class="md-list-item-text standings">
        
        <div class="middle">
            <h3>{{user.name}}</h3>
            <h4>{{user.nickname}}</h4>
        </div>

        <div class="right">
            <span>{{winPercentage}}%</span>
        </div>
    </div>
{{/paper-item}}